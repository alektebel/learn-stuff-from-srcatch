/*
 * Test: If Statement
 * 
 * Tests if-else conditional statements.
 * Tests:
 * - If statement
 * - Else clause
 * - Comparison operators
 * - Return in different branches
 * 
 * Expected: Returns 1 (since 5 > 3)
 */

int main() {
    int x = 5;
    if (x > 3) {
        return 1;
    } else {
        return 0;
    }
}
