# 23 August 2026 — Week 0

Expected publish: *The plan, the track I picked, and a screenshot of 0%.*

## Track

My track: `________`

Started: 23 August 2026

## Baseline

```
python3 progress.py --track core --checks
```

Paste the last lines here. Every bar should read 0%. That is the point.

## Priority, as of today

1. AWS from scratch
2. LLM work (autograd → transformer → distillation → cache → serving)
3. Provenance-reasoning (ℕ[X] → s(CASP) → LINC, then ContextCite / SPADE / MARS-SQL)
4. Distributed training
5. Database engine
6. Everything else

## What "traceable and replicable" will mean in October

English → (untrusted) parse → (deterministic) proof → how-polynomial → any later question is a homomorphism.

The LLM is not allowed to deduce. There is no LLM here anyway: LINC is a gold parser plus a fault injector whose taxonomy is the paper's own error analysis. ContextCite, after that, attributes tokens. Different question.

## Why I am doing this

(write this today)

## What I set up

- [ ] gcc, make, Python 3.11+
- [ ] venv per ML directory, when I get there
- [ ] GPU budget, if I am on core or full (real GPU from week 17)
- [ ] this journal running (`python3 journal/serve.py`)
